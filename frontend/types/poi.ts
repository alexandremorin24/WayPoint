export interface POI {
    id: string
    mapId: string
    name: string
    description?: string
    x: number
    y: number
    categoryId: string
    imageUrl?: string
    createdAt: string
    updatedAt?: string
}
